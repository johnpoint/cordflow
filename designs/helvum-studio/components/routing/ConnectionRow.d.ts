export interface ConnectionRowProps {
  from: string;
  to: string;
  media?: string;
  selected?: boolean;
  action?: React.ReactNode;
}
export declare function ConnectionRow(props: ConnectionRowProps): React.ReactElement;

