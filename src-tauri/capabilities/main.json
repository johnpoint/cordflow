{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the PipeWire graph, device, volume, meter, and frameless-window commands required by the main window. The native system tray exposes no webview command.",
  "windows": ["main"],
  "permissions": [
    "default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
